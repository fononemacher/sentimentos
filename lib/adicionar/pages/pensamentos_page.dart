import 'package:flutter/material.dart';

class TipoPensamentosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: <Widget>[
          Text("Tipo Pensamentos Page"),
        ],
      ),
    );
  }
}
